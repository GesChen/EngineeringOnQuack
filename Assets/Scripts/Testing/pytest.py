from datetime import datetime
startTime = datetime.now()

class test:
	print("innit")

	property = "blah"

	def test(self, x):
		self.a = x
		print("constructed " + str(x))	test

	def something(y):						test 2
		print("this is "+this.property)
		print("found a to be " + str(this.a))
		print(this.a + y)
		print("nuts")

		this.another(this.a, 2, y)
		print("its something "+str(this.fab)+" with pro "+this.property)
	
	def another(a,b,c):
		this.fab = a + b + c
		print(this.fab)

#Python 3: 
print((datetime.now() - startTime).microseconds / 1000)