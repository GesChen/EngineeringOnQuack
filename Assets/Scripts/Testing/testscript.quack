class dictionary:
    keys = []
    values = []

    def dictionary():
        return
    
    def newItem(key, value):
        keys += key
        values += value
    
    def test(a):
        return a+"helo"
    
    def string():
        return "dictionary with keys " + keys + " and values " + values

    log("indict")
    dump()

dump()

log(dictionary)

newdict = dictionary()

newdict.newItem("test", 1)

newdict.test(1)
log(newdict + "test")
log (newdict.keys)

log(newdict.keys.join("test"))


log(4*2*(3-1))