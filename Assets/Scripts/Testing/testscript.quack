class testclass:
    A = 0
    B = 0

    log("hell yeah")

    dump()
    def testclass(a, b):
        A = a
        B = b
        dump()

    def thing():
        log(A + B)

log("done define")

test = testclass(1,2)

test.thing()

test.A = 10
test.thing()

normal = 0
log("nromal"+normal)