class dictionary:
    keys = []
    values = []

    def dictionary():
        return
    
    def newItem(key, value):
        keys += key
        values += value
    
    def string():
        return "dictionary with keys " + keys + " and values " + values

newdict = dictionary()

newdict.newItem("test", 1)

log(newdict)